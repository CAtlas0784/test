---
name: create-skill
description: Workflow for authoring and registering new AI skills within the AI TOOLBOX.
---
# Create Skill Guide
1. Create directory `tools/AI TOOLBOX/skills/<skill-name>/`.
2. Add `SKILL.md` with YAML frontmatter (`name`, `description`) and markdown instructions.
